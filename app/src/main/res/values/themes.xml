<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="Base.Theme.UniRide" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
    </style>

    <!-- Main Theme (with your background and navbar color) -->
    <style name="Theme.UniRide" parent="Base.Theme.UniRide">
        <!-- Background color for activities -->
        <item name="android:windowBackground">@color/background_color</item>
        <!-- Navbar background color -->
        <item name="colorPrimary">@color/navbar_color</item>
    </style>
</resources>
